<!DOCTYPE html>
<head>

    <title> Home </title>
</head>
<body>

    <%- include("layout/header") %>

<main>


<form action="/" method="post">
    <input type="text" placeholder="programming  language " name="planguage">
    <button type="submit">Add</button>
</form>

<ul>
    <% if(shaber.length > 0){ %>
        <% for(let x = 0; x<shaber.length;x++){ %>
            <li><%= shaber[x] %></li>
       <% } %>
    <% }else{ %>
        <p>no data fount</p>
    <% } %>
</ul>

</main>

<%- include("layout/footer") %>

</body>
</html>